<%= form_with scope: :comment, url: "/user/#{params[:user_id]}/post/#{params[:id]}/comment", 
	method: :post, local: true  do |form| %>
	
	<%= form.label :text, 'New comment: '%>
	<%= form.text_field :text%>	
	<%= form.submit 'submit'%>
<%end%>
